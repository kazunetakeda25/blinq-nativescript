android {
            defaultConfig {
                applicationId = "com.fashion.blinq"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }